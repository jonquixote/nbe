# Graphics templates

Template JSON layouts plus packaged font assets. Rendered to GPU textures by `nbe-engine` (SDF/Skia-class text rendering). There is no HTML/browser render path in the engine — see SPEC.md section 5.5.
