{
  "name": "@nbe/control-plane",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "NBE rundown engine and WebSocket command bus (SPEC v0.2 section 4)",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node src/index.js",
    "test": "node --test"
  }
}
